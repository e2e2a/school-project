<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="/public/images/logo_dsf.png" type="image/x-icon">
  <title>Verification Code</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .verification-form {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .verification-card {
      max-width: 400px;
      width: 100%;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>

<div class="container verification-form">
  <div class="verification-card">
    <h3 class="text-center mb-4">New Password</h3>
    <form method="post" action="/new/password/verify">
      <% if (messages.error) { %>
        <div class=" text-danger text-center mb-3" role="alert">
            <p><%= messages.error %></p>
        </div>
      <% } %>
        <input type="hidden" name="token" value="<%= userToken.token %>">
      <div class="mb-3">
        <label class="form-label" for="newPassword">New Password</label>
        <input type="password" class="form-control" id="newPassword" name="newPassword" placeholder="New Password">
      </div>
      <div class="mb-3">
        <label class="form-label" for="confirmPassword">Re-type new Password</label>
        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" placeholder="Re-type new Password">
      </div>
      <button type="submit" class="btn btn-primary w-100 mb-3 submit-button1" name="action" value="submit">Submit</button>
    </form>
  </div>
</div>

<!-- Bootstrap Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
<script src="/public/vendor/jquery-3.2.1.min.js"></script>
<script>
  $(document).ready(function() {
          // Listen to the submit event of forms containing submit-button class
          $('.submit-button1').closest('form').on('submit', function() {
              // Disable the submit button when the form is submitted
              $(this).find('.submit-button').prop('disabled', true);
          });
      });
      $(document).ready(function() {
          // Listen to the submit event of forms containing submit-button class
          $('.submit-button2').closest('form').on('submit', function() {
              // Disable the submit button when the form is submitted
              $(this).find('.submit-button').prop('disabled', true);
          });
      });
  </script>
</body>
</html>